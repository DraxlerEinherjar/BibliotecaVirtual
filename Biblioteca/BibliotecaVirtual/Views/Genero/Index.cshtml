@model IEnumerable<BibliotecaVirtual.Models.Genero>

@{
    ViewBag.Title = "Géneros";
    Layout = "~/Views/Shared/_Layout_two.cshtml";
}

    <section id="services">
        <div class="container wow fadeInUp">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="section-title">Géneros</h3>
                    <div class="section-title-divider"></div>
                    <p class="section-description">
                        Los géneros literarios son los distintos grupos o categorías en que podemos clasificar las obras literarias atendiendo a su contenido y estructura. 
                        La retórica los ha clasificado en tres grupos importantes: narrativo, lírico o épico y dramático, a los que se añade con frecuencia el género didáctico, 
                        convirtiéndose en un punto de referencia para el análisis de la literatura.
                        Así mismo los géneros literarios son modelos de estructuración formal y temática que le permiten establecer un esquema previo a la creación de su obra.

                    </p>
                </div>
            </div>
            <div class="row">
                <p>
                    <a href="@Url.Action("Create", "Genero")" class="btn btn-success"><i class="fa fa-plus"></i><span><strong> Crear</strong></span></a>
                </p>
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nombre)
                        </th>
                        <th></th>
                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nombre)
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "Genero", new { id = item.IdGenero })" class=""><i class="fa fa-pencil"></i><span><strong> Editar</strong></span></a> |
                                <a href="@Url.Action("Details", "Genero", new { id = item.IdGenero })" class=""><i class="fa fa-search"></i><span><strong> Ver</strong></span></a> |
                                <a href="@Url.Action("Delete", "Genero", new { id = item.IdGenero })" class=""><i class="fa fa-trash"></i><span><strong> Eliminar</strong></span></a>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </section>
